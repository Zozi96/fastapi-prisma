datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

generator db {
    provider             = "prisma-client-py"
    interface            = "asyncio"
    recursive_type_depth = 5
}

model Task {
    id          Int     @id @default(autoincrement())
    title       String
    description String?
}
